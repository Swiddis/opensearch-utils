source = big5
| where `agent.type` = 'filebeat'
| where `cloud.region` = 'us-east-1'
| stats count(), max(`metrics.size`) as max_size by `agent.type`, `cloud.region`, `host.name`, `agent.name`
| fields + `agent.type`, `cloud.region`, `host.name`, `agent.name`, `count()`, `max_size`

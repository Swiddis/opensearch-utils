source = big5
| where `agent.name` = 'filebeat' and `@timestamp` >= '2023-01-01 00:00:00' and `@timestamp` <= '2023-01-02 00:00:00' and `metrics.size` > 1000 and `metrics.size` != 5000
| stats count(`agent.name`)

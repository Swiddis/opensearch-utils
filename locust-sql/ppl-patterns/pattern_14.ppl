source = big5
| where `agent.type` = 'filebeat'
| where `@timestamp` >= '2023-01-01 00:00:00' and `@timestamp` <= '2023-01-02 00:00:00'
| sort `@timestamp`
| fields + `@timestamp`, `agent.name`, `agent.type`, `host.name`, `cloud.region`, `message`, `metrics.size`, `metrics.tmin`, `aws.cloudwatch.log_group`, `aws.cloudwatch.log_stream`, `event.id`, `process.name`

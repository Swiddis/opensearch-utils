source = big5
| where `agent.type` = 'filebeat'
| where `@timestamp` >= '2023-01-01 00:00:00' and `@timestamp` <= '2023-01-02 00:00:00'
| stats count(), count(agent.name), count(host.name) by `agent.type`
| fields + `agent.type`, `count()`, `count(agent.name)`, `count(host.name)`

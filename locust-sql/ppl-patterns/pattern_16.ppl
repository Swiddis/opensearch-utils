source = big5
| where `agent.type` = 'filebeat'
| where `cloud.region` = 'us-east-1'
| stats count(), min(metrics.size), max(metrics.size) by `agent.type`, `cloud.region`, `host.name`, `agent.name`, `aws.cloudwatch.log_group`, `process.name`
| sort `count()`
| fields + `agent.type`, `cloud.region`, `host.name`, `agent.name`, `aws.cloudwatch.log_group`, `process.name`, `count()`, `min(metrics.size)`, `max(metrics.size)`
